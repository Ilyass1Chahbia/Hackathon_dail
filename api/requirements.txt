# Runtime requirements for the C04 API on Vercel (Root Directory: api).
# Mirrors the [project].dependencies of pyproject.toml; keep the two in step.
fastapi>=0.115
uvicorn[standard]>=0.32
langgraph>=0.2.60
pydantic>=2.9

# Test-only, listed so `pytest` runs on a clean install (Vercel ignores these at run time).
pytest>=8.3
httpx>=0.27
